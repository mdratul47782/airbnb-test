/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['plus.unsplash.com','images.unsplash.com'], // Add any other domains you need here
    },
  };
  
  export default nextConfig;
  